'use strict';

describe('Service: eventTagger', function () {

  // load the service's module
  beforeEach(module('udbApp'));

  // instantiate service
  var EventTagger;
  beforeEach(inject(function (_EventTagger_) {
    EventTagger = _EventTagger_;
  }));

});
