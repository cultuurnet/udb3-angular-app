'use strict';

describe('Directive: udbJobLog', function () {

  // load the directive's module
  beforeEach(module('udbApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
