'use strict';

describe('Directive: udbDebounce', function () {

  // load the directive's module
  beforeEach(module('udbAppApp'));

  var scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));
});
